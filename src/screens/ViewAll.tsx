import { SText, SView } from '~components';

const ViewAll = () => {
  return (
    <SView display='flex' flex={1} color='primary'>
      <SText>ViewAll</SText>
    </SView>
  );
};

export default ViewAll;
